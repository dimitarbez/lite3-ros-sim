# Lite3 physical emotion work

The emotion implementation tickets are organized under
[`tickets/`](tickets/README.md). Start with the linked index for current status,
dependencies, the exact-neutral transition contract, and one detailed ticket
per emotion.
